# Mutants that cannot be killed because they do not change behaviour. One line
# each, in the form file:line:column:from>to | why.
#
# rencana-uji.md section 5 says a survivor has to be explained one at a time
# rather than waved through, so this file is the place that happens. A mutant
# belongs here only when it is genuinely equivalent. A survivor that is merely
# hard to reach is a missing test, not an entry here.
#
# Line numbers move when the file moves, so an entry that stops matching starts
# counting again, which is the behaviour we want.

src/libraries/ClearingMath.sol:39:29:>>>= | The two branches subtract in opposite orders, so at price equal to ref both give zero and the boundary the mutation moves is the one case where the answer is the same either way.
src/libraries/ClearingMath.sol:52:23:<><= | Picking the smaller of two equal numbers returns that number whichever branch is taken.

src/SessionManager.sol:176:36:<><= | MAX_TRANSITION_STEPS is 64 and it is a guard against a malformed table rather than a working limit. The longest walk the committed calendar produces is a holiday weekend at four boundaries, so one extra iteration of the guard changes nothing any caller can see.
src/SessionManager.sol:210:32:<><= | Choosing between a DST boundary and an intraday mark that fall on the same second returns that second either way.
src/SessionManager.sol:210:20:!=>== | Inside this function the timestamp is already known to be covered by the table, which means the index is below its length and the boundary is never zero. The mutant therefore never takes the DST branch, and that is unobservable because both annual shifts land at 02:00 on a Sunday, which is CLOSED_WEEKEND on both sides. The offset is recomputed from the timestamp at every step rather than carried across one, so skipping the boundary does not carry the winter offset into the summer. If a shift ever moved to a weekday this stops being equivalent.
src/SessionManager.sol:215:21:<><= | Reachable only past the end of the table, and the caller returns zero before it gets here whenever the timestamp is not covered. The out of bounds read the mutant introduces has no path to it.
src/ClearingVerifier.sol:152:28:<><= | Picking the smaller of demand and supply returns the same number when they are equal, which is also the case the crossing price is chosen for.

src/Settlement.sol:369:34:>>>= | The wedge is the balance minus what was there before. When the two are equal both branches give zero, and equal is the only case the mutation moves.
src/Settlement.sol:376:62:*>/ | MIN_SAVINGS_BPS is 1, so multiplying the notional by it and dividing the notional by it produce the same number. This stops being equivalent the moment the threshold moves off one basis point.
src/Settlement.sol:388:27:>>>= | SOLVER_SHARE_OF_FEE_BPS is 7500, so the solver share of any positive wedge is strictly less than the wedge, which leaves the treasury share strictly positive. The loop has already skipped the zero wedge case by then, so the branch is never reached with zero.
src/Settlement.sol:450:32:>>>= | When the routed volume equals the notional there is nothing netted either way, and equal is the only case the mutation moves.

src/PriceOracle.sol:113:38:>>>= | The age floors at zero, so at a stamp equal to the block both branches give zero and equal is the only case the mutation moves.
src/PriceOracle.sol:214:48:>>>= | The segment starts at the later of the round and the window, and when the two are the same second both branches name that second.
src/PriceOracle.sol:215:31:>>>= | A segment of zero length weighs zero and covers zero, so adding it changes neither the numerator nor the denominator, and the cursor it moves is already where it would move to.
src/PriceOracle.sol:222:30:<=>< | A round stamped exactly on the window start has already been counted and has already contributed every second it can. Walking one round further adds a segment that ends where it starts, which is nothing, and that round is outside the window so it counts nothing either.
src/PriceOracle.sol:255:22:<><= | The walk stops at the first OPEN second or at a table that has run out, and the day after next is two days of transitions away. One more iteration of a loop that has already returned or broken is unreachable.
src/PriceOracle.sol:269:19:<=>< | A feed answer of zero converts to zero either way, because the scaling below multiplies or divides a zero and gets a zero back.
src/PriceOracle.sol:273:24:<><= | Eighteen decimals returned on the line above, so the comparison here never sees equality.
src/PriceOracle.sol:278:25:>>>= | The absolute difference of two equal numbers is zero whichever order they are subtracted in.
src/PriceOracle.sol:279:40:>>>= | The larger of two equal numbers is that number whichever branch is taken.
src/PriceOracle.sol:281:19:>=>> | The cap and the value are the same number at the boundary, so returning the cap and casting the value produce the same sixteen bits.
src/PriceOracle.sol:258:34:<=>< | nextTransition answers the first second after its argument whose session differs, so it is either zero or strictly ahead of the cursor. The equality this mutation moves is a guard against a malformed table rather than a case the committed calendar reaches.
src/PriceOracle.sol:258:26:||>&& | A next of zero satisfies both sides of the and, so the walk still stops when the table runs out. The only case the two forms disagree on is a transition at or behind the cursor, which nextTransition does not produce.
src/PriceOracle.sol:277:19:||>&& | With one side missing the mutant measures ten thousand basis points instead of the sixteen bit ceiling. Every caller compares that number against a limit of at most fifteen hundred, so both answers land on the same side of every comparison in this contract. A limit above ten thousand basis points would end that.
